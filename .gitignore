# Audio recordings (kept locally, not in repo)
*.m4a
*.wav
*.mp3

# Auto-generated by Soul OS harness — regenerated each session
CLAUDE.md
GEMINI.md

# macOS
.DS_Store

# Node
node_modules/

# Astro / Vite build output and caches
www/dist/
www/.astro/
**/.vite/
**/.cache/

# Wrangler (Cloudflare) caches — auth + Pages metadata, never commit
.wrangler/
**/.wrangler/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Local-only history backups (contain pre-public-release private content)
.git.private-backup/

# Claude Code project state
.claude/
